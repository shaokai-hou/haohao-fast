import request from '@/utils/request'

export function getRolePage(query) {
  return request({
    url: '',
    method: 'get',
    params: query
  })
}

export function getRoleList(query) {
  return request({
    url: '',
    method: 'get',
    params: query
  })
}
